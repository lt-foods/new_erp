-- ============================================================
-- 資料修正（第二張）：PO2606100406 被斷貨自動結案鎖住的貨放回派貨流程
--
-- 承 20260819010000（PO2607241023）。同一個病症、同一種修法。
-- 不改 schema、不改任何函式，只修一張單的 status 欄位。
-- 留成 migration 是為了讓正式庫的人工修正有跡可循。
--
-- ── 病症（摘要，詳見 20260819010000 檔頭）──────────────
-- `_refresh_po_status`（20260801000000:386-399）：
--   「有任一品項斷貨」＋「其餘品項收齊」＋「有到貨」
--   → 收貨（rpc_confirm_gr）當下自動把整張 PO 設成 closed
-- 而 closed 不在派貨需求 view 白名單（20260818000030:77）
--   → 已收到的貨從派貨工作台消失 → 建不了波次 → 沒有 transfer
--   → 客人取不到貨、月結也收不到錢
--
-- ── 這張單 ──────────────────────────────────────────────
-- PO2606100406：G00185-01 日本 BIG BIO 排水口黏滑異味分解錠 訂 90 / 收 90（收齊），
-- 同單另有 1 項斷貨 → 整張被自動結案。
-- 全單卡住 139 件、**54 張客人訂單**在等（本波盤點中客單數最多的一張）。
--
-- 選 partially_received 而非 fully_received：全單並未全到（斷貨那項收 0），
-- 標 fully_received 會讓單據說謊。partially_received 正是 `_refresh_po_status`
-- 在沒有 closed 分支時本來就會給的狀態（同檔 :391-392），一樣在派貨白名單內。
--
-- ── 安全性 ──────────────────────────────────────────────
-- · purchase_orders 只有一個 BEFORE UPDATE trigger（trg_touch_purchase_orders，
--   只更新 updated_at），沒有阻擋或連動邏輯
-- · WHERE 同時比對 po_no 與 status='closed' → 只命中這一張；冪等，重跑無作用
-- · 斷貨品項的 stockout_at 不動 → 它仍然是斷貨，不會被誤認為還在等貨
-- · 在沒有這張單的環境（staging / 新建庫）WHERE 不命中，等同 no-op
--
-- ── 正式庫執行紀錄（已驗收）────────────────────────────
--   ✅ 已改成 partially_received
--   ✅ 1 項斷貨品項的標記還在（沒被動到）
--   ✅ 已回到派貨工作台（23 列需求）
--   ✅ G00185-01 可派量：0 → 90 件
--
-- ── 已知副作用 ──────────────────────────────────────────
-- 1. 這張單回到「部分到貨」，採購單列表會重新出現「進貨」按鈕
-- 2. ⚠️ 之後若有人對它按收貨或調整已收量，_refresh_po_status 會再跑
--    → 又變回 closed（根因未修）。故修正後應儘快完成派貨
-- 3. 派貨動作本身不會觸發 _refresh_po_status，派完不會變回去
--
-- ── 回滾 ────────────────────────────────────────────────
--   UPDATE purchase_orders SET status = 'closed'
--    WHERE po_no = 'PO2606100406' AND status = 'partially_received';
--
-- ── 規模與根因（本 PR 不動）────────────────────────────
-- 這不是個案：正式庫盤點出符合同一病症、且還有客人在等的單共 **34 張**，
-- 涉及 **580 張客人訂單**、約 4,241 件貨，橫跨 2026-06-03 ～ 08-07。
-- 已開需求單：公司\01_進行中\需求暨計畫_斷貨連坐鎖貨根治_2026-08-19.md
-- 推薦方向：`_refresh_po_status` 斷貨分支改判 fully_received —— 語意上
--   「該收的都收到了」，且 view 的 qty_shortage 欄位本就處理短收
--   （20260818000030:245）。
-- ⚠️ 採購單狀態白名單被 **27 支** view / RPC 當閘門條件（grep 全歷史去重），
--    20260812000000 檔頭已警告「漏一個就是靜默錯帳」，需逐支比對後才動。
-- ============================================================

UPDATE purchase_orders
   SET status     = 'partially_received',
       updated_at = NOW()
 WHERE po_no = 'PO2606100406'
   AND status = 'closed';
